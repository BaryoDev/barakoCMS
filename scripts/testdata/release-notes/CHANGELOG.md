# Changelog

## [Unreleased]

## [9.2.0] - 2026-02-01

### Fixed

- **A short release.** Its notes fit, so they are printed exactly as written.

## [9.1.0] - 2026-01-02

A release with more notes than the limit the test sets.

### Breaking

- **A breaking entry that must survive in full.** A reader has to act on this one, so it is never
  replaced by a count, however long the rest of the section is.

### Added

- **Added entry one.** Filler that pushes the section over the limit the test sets, so the script
  has to summarise it rather than print it whole.
- **Added entry two.** Filler that pushes the section over the limit the test sets, so the script
  has to summarise it rather than print it whole.
- **Added entry three.** Filler that pushes the section over the limit the test sets, so the script
  has to summarise it rather than print it whole.

### Fixed

- **Fixed entry one.** Filler that pushes the section over the limit the test sets, so the script
  has to summarise it rather than print it whole.
- **Fixed entry two.** Filler that pushes the section over the limit the test sets, so the script
  has to summarise it rather than print it whole.

### Removed

- **The only removed entry.** Filler that pushes the section over the limit the test sets, so the
  script has to summarise it rather than print it whole.

### Security

- **A security entry that must survive in full.** Also something a reader has to act on.

## [9.0.0] - 2026-01-01

### Breaking

- **A breaking entry too long to fit on its own.** Filler that keeps going so that even with every
  other subsection reduced to a count, this one alone is over the limit the test sets. Filler that
  keeps going so that even with every other subsection reduced to a count, this one alone is over
  the limit the test sets. Filler that keeps going so that even with every other subsection reduced
  to a count, this one alone is over the limit the test sets. Filler that keeps going so that even
  with every other subsection reduced to a count, this one alone is over the limit the test sets.
  Filler that keeps going so that even with every other subsection reduced to a count, this one
  alone is over the limit the test sets. Filler that keeps going so that even with every other
  subsection reduced to a count, this one alone is over the limit the test sets. Filler that keeps
  going so that even with every other subsection reduced to a count, this one alone is over the
  limit the test sets. Filler that keeps going so that even with every other subsection reduced to a
  count, this one alone is over the limit the test sets. Filler that keeps going so that even with
  every other subsection reduced to a count, this one alone is over the limit the test sets.

### Fixed

- **A fixed entry.** Short.
