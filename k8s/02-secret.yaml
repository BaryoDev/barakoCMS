apiVersion: v1
kind: Secret
metadata:
  name: barako-secrets
  namespace: barako-cms
type: Opaque
stringData:
  # ⚠️  WARNING: These are PLACEHOLDER values for local testing only!
  # For production, use sealed-secrets or external-secrets.
  # NEVER commit real secrets to git!
  JWT__Key: "REPLACE_THIS_WITH_A_REAL_32_CHAR_SECRET_KEY"
  POSTGRES_PASSWORD: "REPLACE_THIS_WITH_REAL_PASSWORD"
