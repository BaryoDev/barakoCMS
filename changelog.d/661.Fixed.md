- **The 3.x upgrade gate went green while the published image refused to start.**
  `scripts/upgrade-check.sh` ran the core host, which loads no module, so no module's schema was
  ever checked. The image runs the Suite, and on the playground it refused to start on
  `Files: public.mt_doc_stored_files`, an index 3.x never created. The gate now checks the schema
  with both hosts and boots the Suite, and `docs/upgrading-to-4.0.md` names the second file an
  upgrade needs, `migrations/4.2.0/stored-files-parent-index.sql`. Running the Suite also showed the
  rollback could not work for it: 3.21.0 refused to start on that index, so
  `migrations/4.0.0/rollback-to-3.x.sql` now drops it. The gate also stops the 4.0 host it starts:
  it used to kill a subshell and leave dotnet running through the rollback.
