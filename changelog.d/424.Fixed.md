- **The sweep-versus-editor concurrency test now reports which side failed.** It set one flag on
  entering its hook and let anything the editor threw propagate, and the sweep's catch matches any
  exception whose type name contains "Concurrency", so an editor append that lost its own race was
  swallowed by the sweep and read as the sweep winning. The test then failed on the title with a
  message about the sweep overwriting an edit that never landed. It keeps the editor's exception and
  asserts on it by name before the outcome assertions. This does not fix the intermittent CI failure
  in #424; it makes the next one say what happened.
