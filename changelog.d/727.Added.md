- **The only deployment guide was for a VM.** An agency that does not want to run servers found
  nothing on App Service, Fargate or Cloud Run and could fairly conclude those were unsupported.
  `docs/deploy-on-a-managed-platform.md` covers the image, the environment variables, the managed
  database, the health probes, file storage and what scaling out coordinates and what it does not,
  with each application claim checked against the code or the published `4.1.0` image.
