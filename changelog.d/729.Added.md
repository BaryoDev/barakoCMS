- **There was no path from a fresh clone to a working module.** `MODULES.md` is the contract, written
  for someone who already knows the codebase. `docs/your-first-module.md` now walks through building
  a small module in this repository: the project, the module class, a lifecycle hook that refuses a
  write, a capability-gated endpoint, tests including the authorisation ones, and calling it on a
  running host.
