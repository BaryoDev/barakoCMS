D20 records where each of the four products is extended: barakoCMS by modules, barakoPress by
widgets, BaryoVM by release manifests, and barakoBrew by nothing. It also writes down the ladder a
developer climbs, where a module is the last resort rather than the first move, and the reason
barakoBrew deliberately has no plugin model.
