# BarakoCMS Environment Variables
# Copy this file to .env and update with your actual values
# NEVER commit .env to source control!

# Database Connection
DATABASE_CONNECTION_STRING=Host=localhost;Database=barako_cms;Username=postgres;Password=postgres

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-key-minimum-32-characters-long-change-in-production-immediately

# Email Service (if using SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# SMS Service (if using Twilio)
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# Environment
ASPNETCORE_ENVIRONMENT=Development

# Optional: Application URLs
# ASPNETCORE_URLS=https://+:5001;http://+:5000
